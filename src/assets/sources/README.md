# Credential source logos

Place predefined source PNG files here using the stable names declared in
`src/features/credential-vault/models/credential-vault.ts` (for example,
`github.png`, `supabase.png`, and `aws.png`). The Credential Vault falls back to
a generic source mark when an image is not present, so source records do not
depend on a bundled image existing.
